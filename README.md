# DownloadMercy 📥

**DownloadMercy** — это быстрый и удобный self-hosted веб-интерфейс для скачивания видео, аудио (MP3) и превью с YouTube, Instagram, TitTok, Pinterest и множество других платформ. Проект построен на связке **Node.js (Fastify)**, **BullMQ** и мощного бэкенда **yt-dlp**.

## 🚀 Основные возможности

* **Скачивание видео:** поддержка выбора качества (1080p, 720p и т.д.) с автоматической склейкой в MP4.
* **Скачивание звука с видео (mp3):** извлечение аудиодорожки в лучшем качестве.
* **Превью:** сохранение обложек видео в формате JPG.
* **Фоновые задачи:** благодаря Redis и BullMQ, скачивание происходит в фоне, а пользователь видит прогресс в реальном времени.
* **Авто-очистка:** встроенный «уборщик» удаляет старые файлы через заданное время, чтобы не забивать диск.
* **Безопасность:** система удаляет только файлы с префиксом `dmercy_`, не трогая ваши личные данные даже при неверной настройке папок.

---

## 🐳 Запуск через Docker (Рекомендуется)

Это самый простой и быстрый способ.

### 1. Клонирование

Скачайте репозиторий:
```bash
git clone [https://github.com/your-username/download_mercy.git](https://github.com/your-username/download_mercy.git)
cd download_mercy
```

### 2. Подготовка окружения

Создайте файл конфигурации из примера:
```bash
cp .env.example .env
```
*Настройки по умолчанию в файле .env уже оптимизированы для запуска в Docker.*

### 3. Запуск

Поднимите контейнеры в фоновом режиме (убедитесь, что вы находитесь в корневой директории проекта:
```bash
docker-compose up --build -d
```

Откройте браузер и перейдите по адресу:👉 http://localhost:3000

---

## 💻 Локальный запуск (без Docker)

## 🛠 Зависимости (Важно!)

Если вы планируете запускать проект **без Docker**, вам необходимо установить следующие инструменты:

1. **Node.js**: Версия 20 или выше.
2. **Redis**: Обязательно для работы очереди задач.
3. **yt-dlp**: Главный инструмент для загрузки. Он должен быть доступен в вашей системе под именем `yt-dlp` или просто находится в папке с проектом (или настроен через `.env`).
4. **FFmpeg**: **Критически важно** для склейки видео и аудио, а также для конвертации в MP3. Без него многие функции не будут работать.
5. **Python 3**: Требуется для работы самого `yt-dlp`.

### 1. Клонирование

Скачайте репозиторий:
```bash
git clone [https://github.com/your-username/download_mercy.git](https://github.com/your-username/download_mercy.git)
cd download_mercy
```

### 2. Подготовка окружения

Создайте файл конфигурации из примера:
```bash
cp .env.example .env
```
*Настройки по умолчанию в файле .env уже оптимизированы для запуска в Docker.*

### 3. Установка зависимостей

Вам нужно установить пакеты для API и для Воркера отдельно:

```bash
cd api && npm install
cd ../worker && npm install
cd ..
```

### 4. Запуск

Убедитесь, что ваш **Redis** запущен. Затем откройте два терминала:

**Терминал 1 (API сервер):**

```bash
cd api
npm start
```

**Терминал 2 (Воркер):**

```bash
cd worker
npm start

```

Теперь откройте в браузере `http://localhost:3000`.

---

## 📂 Структура проекта

* `/api` — Fastify сервер, принимает заказы и отдает файлы.
* `/worker` — Обработчик задач, который запускает `yt-dlp` и следит за чистотой.
* `/public` — Фронтенд на чистом JS, HTML и CSS.
* `/downloaded` — Папка, куда временно сохраняются файлы (игнорируется гитом).

---

## ⚠️ Предупреждения

* **Дисковое пространство:** Следите за параметром `FILE_TTL_HOURS`. Скачанные видео могут занимать много места.
* **Очистка:** Воркер автоматически удаляет файлы, начинающиеся с `dmercy_` в папке загрузок, которая указывается в .env. Не сохраняйте свои личные файлы с таким префиксом в эту папку.
